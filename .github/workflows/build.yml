# ============================================================
# Life Log — 自动打包 & 发布 APK
# ============================================================
# 触发条件：推送 v* 格式的 tag（如 v1.1.0）
# 产出物：arm64 APK 上传到 GitHub Release
# ============================================================

name: Build & Release APK

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# 防止同时运行多个构建
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: 📦 Build Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Validate Supabase secrets
        run: |
          test -n "${{ secrets.SUPABASE_URL }}" || (echo "Missing SUPABASE_URL secret" && exit 1)
          test -n "${{ secrets.SUPABASE_ANON_KEY }}" || (echo "Missing SUPABASE_ANON_KEY secret" && exit 1)

      # ── 1. 拉取代码 ──────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ── 2. 安装 Java 17（Gradle 需要）──────────────────
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      # ── 3. 安装 Flutter ───────────────────────────────
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.5'
          channel: 'stable'
          cache: true

      # ── 4. 恢复签名密钥（从 GitHub Secrets 解码）────────
      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | tr -d '\n ' | base64 --decode > android/app/upload-keystore.jks

      - name: Create key.properties
        run: |
          cat > android/key.properties << EOF
          storePassword=${{ secrets.STORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          storeFile=upload-keystore.jks
          EOF

      # ── 5. 安装依赖 ──────────────────────────────────
      - name: Install dependencies
        run: flutter pub get

      # ── 6. 代码生成（Isar .g.dart）────────────────────
      - name: Run build_runner
        run: dart run build_runner build --delete-conflicting-outputs

      # ── 7. 静态分析 ──────────────────────────────────
      - name: Analyze code
        run: flutter analyze --no-fatal-infos

      - name: Build APK (split per ABI)
        run: >
          flutter build apk --release --split-per-abi
          --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }}
          --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}

      - name: Rename release APK files
        run: |
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk build/app/outputs/flutter-apk/lifelog-arm64-v8a-release.apk
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk build/app/outputs/flutter-apk/lifelog-armeabi-v7a-release.apk
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk build/app/outputs/flutter-apk/lifelog-x86_64-release.apk

      # ── 9. 提取版本号用于 Release 标题 ────────────────
      - name: Extract version from tag
        id: version
        run: echo "TAG_NAME=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      # ── 10. 创建 GitHub Release 并上传 APK ────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "LifeLog ${{ steps.version.outputs.TAG_NAME }}"
          body: |
            ## 📱 LifeLog ${{ steps.version.outputs.TAG_NAME }}

            ### 下载安装包
            - **lifelog-arm64-v8a-release.apk** (推荐，适用于绝大多数现代手机)
            - **lifelog-armeabi-v7a-release.apk** (旧款32位手机)
            - **lifelog-x86_64-release.apk** (模拟器/特殊设备)

            ### 安装方式
            1. 下载对应你手机架构的 APK 文件
            2. 传输到手机并安装（可能需要允许"未知来源"）

            ---
            *此版本由 GitHub Actions 自动构建*
          draft: false
          prerelease: false
          files: |
            build/app/outputs/flutter-apk/lifelog-arm64-v8a-release.apk
            build/app/outputs/flutter-apk/lifelog-armeabi-v7a-release.apk
            build/app/outputs/flutter-apk/lifelog-x86_64-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
